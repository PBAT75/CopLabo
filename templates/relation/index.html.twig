{% extends 'base.html.twig' %}

{% block title %}Relation index{% endblock %}
{% block titre %}Les rencontres{% endblock %}
{% block body %}

    <table class="table">
        <thead>
            <tr>
                <th>Personne1</th>
                <th>Personne2</th>
                <th>Evénement</th>
                <th>Date</th>
            </tr>
        </thead>
        <tbody>
        {% for relation in relations %}
            <tr>
                {#{% for user in relation %}#}
                {{ dump(relation) }}
                {% for person in persons %}
                    {#<td>{{ relation.user1.person}} {{ person.lastname}}</td>#}
                {% endfor %}
                {#{% endfor %}#}
                <td>{{ relation.event }}</td>
                <td>{{ relation.date|date("m/d/Y") }}</td>
                <td>
                    <a href="{{ path('relation_show', {'id': relation.id}) }}">show</a>
                    <a href="{{ path('relation_edit', {'id': relation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>

        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('relation_new') }}">Create new</a>
{% endblock %}

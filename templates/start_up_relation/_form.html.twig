{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-danger m-2">{{ button_label|default('Valider votre action') }}</button>
{{ form_end(form) }}
